version: "3.9"

services:

  web:

    build: .

    container_name: rental_home

    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "8000:8000"

    restart: always